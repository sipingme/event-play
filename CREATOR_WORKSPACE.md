# 个人创作空间：第一阶段

## 已实现

- 邮箱密码注册、登录、登出；注册后自动获得独立个人工作空间。
- 密码使用带随机盐的 scrypt；数据库仅存会话摘要，登录最长 7 天，退出即时撤销该会话。
- 登录内修改密码，撤销所有旧设备会话并签发新会话。
- Next.js 同源 BFF 使用 HttpOnly、SameSite=Lax Cookie；生产环境启用 Secure，写请求验证 Origin，响应禁止缓存。
- 作品、品牌、编排和云场次通过后端工作空间归属检查隔离。品牌跨设备同步。
- 游戏体验库「制作同款」：未登录时保留模板参数进入登录/注册；登录后复制配置到私有草稿。
- 新建默认从「欢乐摇摇赛马」开始；复制其他模板时保留专属玩法参数。
- 保存草稿、独立试玩、发布私有配置版本、创建正式场次；已开场配置不会随草稿修改而改变。
- 发布不等于公共上架。官方模板不会被用户修改覆盖。
- 主持凭证按账号划分浏览器存储；账号切换重新加载工作台以清除页面缓存。

## 使用路径

1. 在 `/register` 创建个人账号（密码 10～128 位）。
2. 在 `/dashboard/templates` 体验「欢乐摇摇赛马」，点击「制作同款」。
3. 修改名称、品牌 Logo、队伍、时长等参数，保存草稿。
4. 在发布页进入独立试玩场次，可从主持页打开大屏并扫码验证 H5 玩家端。
5. 返回作品发布私有版本，再进入联机主持端创建/接管正式场次。
6. 其他设备用同一账号登录，在「我的活动」继续编辑或从云工作区接管场次。

## 兼容与数据

- 原 SQLite 数据保留；新增 accounts、account_sessions、account_attempts、workspace_brands 表，不覆盖旧表。
- 原本地活动和旧密钥工作区不自动归属新账号、不自动导入。
- 云工作区提供用户主动选择的本地草稿导入；导入后需重新发布。
- 旧密钥工作区仍可在非账号模式使用；尚未实现旧密钥空间的账号认领。
- 本机旧彩排历史与账号模式彩排按账号隔离；正式联机场次以云工作区历史为准。
- 公共玩家、大屏仍使用不可猜测的场次入口和原有角色凭证；分享入口的人可访问该场次公开信息。

## 本地与部署配置

后端：`python -m uvicorn main:app --host 127.0.0.1 --port 8001`（apps/api）。
前端：`npm run dev -- --hostname 127.0.0.1 --port 4180`（apps/admin）。

前端服务端环境变量：

- `EVENTPLAY_API_INTERNAL_URL`：开发默认 http://127.0.0.1:8001；生产默认 http://127.0.0.1:8002。
- `EVENTPLAY_APP_ORIGIN`：生产部署应指定 https://event.siping.me，用于同源写请求校验。

本地使用开发模式；生产模式必须通过 HTTPS 访问，不能为 HTTP 调试关闭 Secure Cookie。
此阶段代码未自动部署，不修改现有服务器访问保护。

## 验证

- `python -m unittest discover -v`：含账号隔离、跨账号写入/发布/接管拒绝、会话过期、退出、改密撤销、跨设备品牌、重启持久化、草稿快照、旧数据不混入测试。
- `npm test`：含同源 BFF 路由、失效不回退、账号主持凭证命名空间和模板完整复制测试。
- `npm run typecheck` 与 `npm run build -- --webpack`。

## 尚未实现 / 上线边界

- 自助邮件找回、邮箱所有权验证、微信登录、MFA、团队成员与角色邀请。
- 忘记密码页面明确提示尚未发送邮件；请勿将未验证邮箱作为已验证身份或用于敏感通知。
- 任意背景/角色/音频素材上传、拖拽场景、AI 生成、规则工作流、公开模板市场和审核。
- 当前仍使用单进程 SQLite 运行方式；分布式扩容、生产风控、IP 级入口限流、审计与邮件服务需要下一阶段完成。
- 账号认证有数据库持久化的按账号及全局尝试限流、密码哈希并发上限；不能代替生产网关风控。
- 当前主持人房间凭证是独立的能力凭证。退出账号会清理本机账号主持凭证，但已导出的房间凭证需通过云工作区「接管主持」使其失效。
