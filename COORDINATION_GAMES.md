# 手眼协调游戏：本地可试玩版

八款均可从模板库预览、创建试玩、扫码加入、创建活动与发布。原有萌鼠出没兼容未设置 reactionVariant 的历史活动。

| 模板 | 规则 | 积分 |
|---|---|---|
| reaction-mole | 每 2 秒随机洞口，一轮一次点击 | 命中 1 |
| reaction-rhythm | 本机显示来回移动的节奏光标，绿色区域敲鼓 | 精准 3 / 擦边 1 |
| reaction-stack | 移动楼层与基座对齐，点击落下 | 精准 3 / 擦边 1 |
| reaction-basket | 力度条到达目标区域时投篮 | 命中 2 |
| reaction-fruit | 一次直线滑动切中苹果，避开炸弹 | 成功 1 |
| reaction-chef | 按服务器随机订单顺序选择三份食材 | 完成 3 |
| reaction-fish | 浮标进入目标区域时收竿 | 成功 2 |
| reaction-memory | 记忆 2 秒，6 张卡片，最多 6 次配对 | 每对 1 |

## 美术

与赛马统一为明亮 2.5D 卡通插画。复用现有花园、舞台、运动场、海湾、小镇背景，新生成萌鼠、鼓、投篮猫咪、苹果、厨师熊、金鱼透明素材。原图保留，项目副本在 `apps/admin/public/games/coordination/`，完整内置 imagegen 提示词见该目录 ART-DIRECTION.md。

大屏按队伍展示角色与实时积分；叠楼按累计积分长高（最多展示 6 层，完整得分仍显示），记忆游戏点亮展示卡，星光响应积分更新。手机端展示本人的挑战，不需要盯大屏精确操作。

## 判定与边界

- 服务器生成玩家专属挑战令牌、目标、订单和配对牌；请求不接收客户端分值。
- 时机类按客户端操作耗时计算光标位置，并与服务器耗时进行 650ms 范围校验，以容忍传输延迟。不是严格竞技反作弊方案，不能用于高价值奖品公平性保证。
- 水果当前为单目标直线切割，不是多水果飞行物理版；节奏当前为时机敲击，不含音乐谱面；叠楼当前为对齐积分版，不是物理倒塌模拟。
- 配对牌在记忆阶段必须下发到浏览器，因此不防开发者工具读取。后续正式竞赛版本需单独设计反作弊策略。
- 暂停、结束、断线禁止操作；恢复后重新领取挑战。旧令牌、超时及重复结算拒绝。玩法配置随草稿和发布快照保留。
- 本轮没有服务器部署或 GitHub 提交。

## 验证

- API: `python -m unittest discover -v`
- Frontend: `npm test` / `npm run build -- --webpack`
- Browser: `tests/browser-coordination.cjs`（隔离 UI4191 + 内存 API8012）：八款封面/大屏、窄屏、真实操作得分、暂停、从模板创建；`browser-templates.cjs` 保持五类回归。
