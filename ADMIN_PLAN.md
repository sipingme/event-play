# EventPlay 管理端：技术架构与页面功能规划

状态：管理端前端演示已实现，生产能力待接入 · 日期：2026-09-15

实施更新：已选定 Kiranism Next shadcn Dashboard Starter，使用 Next.js App Router、shadcn/ui、TanStack Query 与 TanStack Form。已完成范围和运行方式以 `apps/admin/EVENTPLAY.md` 为准；下文后端接口、权限与发布要求仍是目标规格，末尾模板比较为历史评估。

## 1. 管理端定位

管理端是活动负责人的工作台，围绕“创建 → 预览 → 发布 → 现场控制 → 结果复盘”组织界面。首屏突出一句话创建、近期活动和继续操作入口。数据统计服务于具体活动，首期不建设独立的复杂 BI 首页。

面向活动执行人员、企业行政和市场人员。页面使用“玩法、主题、队伍、时长”等语言，不向普通用户展示 DSL、WebSocket 或模型参数。

范围包括活动配置与主持人控制；玩家操作在小程序中完成，大屏通过独立播放器展示。

## 2. 前端技术架构

| 层 | 选型 | 分工 |
| --- | --- | --- |
| 应用 | Next.js App Router + TypeScript | 路由壳、客户端交互和生产构建 |
| UI | shadcn/ui + Tailwind CSS | 组件源码、统一主题、表单和反馈 |
| 路由 | Next.js App Router | 页面导航、嵌套布局、URL 参数 |
| 服务端数据 | TanStack Query | 查询、缓存、刷新和变更请求 |
| 表单 | TanStack Form | 复用模板 useAppForm，处理字段、脏状态和提交 |
| 本地状态 | React state / reducer / Context | 编辑面板、弹窗、预览及低频会话上下文 |
| 网络 | 封装原生 fetch + WebSocket 客户端 | HTTP 错误统一处理、房间订阅和重连 |
| 游戏预览 | 嵌入独立大屏播放器 | 复用正式渲染，不在管理端复制游戏逻辑 |

首期不引入 Next.js；管理端按静态应用部署，由 FastAPI 提供业务能力。框架与依赖版本在编码时锁定。

### 数据分工

- 活动列表、活动版本、资源、报告：TanStack Query 管理。
- 正在编辑但未提交的字段：表单管理，不直接改查询缓存。
- 搜索、筛选、分页：保存在 URL，刷新和分享后可恢复。
- 活跃房间的阶段、人数、分数：由专用实时订阅维护单一快照，不同时在多个状态容器内维护不同副本。
- 面板开关、预览模式：本地组件状态。
- 后端返回的权限决定可用操作，前端隐藏按钮仅改善体验，不承担安全校验。

### 逻辑结构

```text
页面与布局
    ↓
业务模块：活动 / 编辑器 / 品牌 / 生成任务 / 现场控制 / 报告
    ↓
数据访问：HTTP 查询与变更 / 房间订阅 / 预览通信
    ↓
FastAPI：鉴权、版本、发布、生成任务、计分与结算
```

### 计划代码组织

```text
apps/admin/src/
├── app/                 路由、Provider、应用布局
├── pages/               页面入口及组合
├── features/
│   ├── auth/
│   ├── activities/
│   ├── editor/
│   ├── generation/
│   ├── brands/
│   ├── live-control/
│   └── reports/
├── components/ui/       shadcn/ui 基础组件
├── components/shared/   状态提示、上传、页面标题等
├── lib/                 HTTP、WebSocket、错误处理
└── styles/              设计变量与基础样式
```

仅为结构规划，本次不创建代码目录。

## 3. 导航与页面清单

首期一级导航：工作台、我的活动、玩法模板、品牌素材。报告从活动进入；账户设置位于用户菜单。生成任务嵌在活动流程中，不设独立的技术任务中心。

以下为前端路由规划，不是现存地址。

| 页面 | 路由 | 优先级 | 主要目的 |
| --- | --- | --- | --- |
| 登录 | /login | 必须 | 管理员进入工作台 |
| 注册 | /register | 必须 | 创建主办方账号 |
| 邮箱验证 | /verify-email | 必须 | 验证邮箱、重发验证邮件 |
| 忘记密码 | /forgot-password | 必须 | 请求密码重置邮件 |
| 重置密码 | /reset-password | 必须 | 验证一次性凭证并设置新密码 |
| 首次设置 | /onboarding | 必须 | 确认工作区名称并进入创建活动 |
| 工作台 | / | 必须 | 一句话创建、继续最近活动 |
| 我的活动 | /activities | 必须 | 查找、复制和管理活动 |
| 新建活动 | /activities/new | 必须 | 输入需求、选玩法、上传品牌 |
| 活动编辑器 | /activities/:id/edit | 必须 | 预览与调整配置 |
| 发布与入场 | /activities/:id/publish | 必须 | 校验、发布、创建现场房间 |
| 现场控制台 | /rooms/:roomId/control | 必须 | 主持人执行活动 |
| 活动报告 | /activities/:id/report | 必须 | 按局次查看结果、生成成绩卡 |
| 玩法模板 | /templates | 基础版 | 理解赛马/拔河并套用 |
| 品牌素材 | /brands | 基础版 | 保存和复用品牌信息 |
| 账户设置 | /settings | 基础版 | 账户信息、退出等 |

“必须”页面按开发计划分批实现，不要求第一轮全部上线。模板市场、成员邀请后台、计费、平台运营后台后置。

## 4. 页面功能详解

### 4.1 登录、注册与账号恢复

- 首期提供邮箱与密码注册、登录；此处“管理员”指主办方工作区管理员，不授予平台超级管理员权限。
- 注册字段：邮箱、密码、确认密码；显示服务条款和隐私说明入口。名称和工作区信息在首次设置中补充。
- 注册成功发送邮箱验证邮件，验证完成后创建默认工作区并将用户设为该工作区所有者；操作幂等，重复验证不能创建多个工作区。
- 未验证账号进入验证提示页，可更正邮箱或限频重发；正式发布、资源上传和付费模型调用要求已验证身份。
- 登录页提供注册与忘记密码入口、密码显隐和提交状态。首期不展示尚未接入的 Google/GitHub 等第三方登录按钮。
- 忘记密码提交邮箱后显示统一发送提示；重置凭证一次性、限时有效，重置成功撤销旧会话并返回登录。
- 密码由后端安全哈希保存，验证与重置凭证不以明文持久化；注册、登录、邮件发送和校验均限频。
- 邮件发送失败可重试；开发环境可使用测试邮件箱，正式上线前必须验证真实送达。
- 显示登录失败、会话过期和无权限状态。
- 登录后返回原目标页面；返回地址限定为站内路径。
- 玩家必须微信登录与管理员登录是两条身份流程，不能混为同一授权。

验收：刷新仍能恢复有效会话；退出清理查询与房间订阅；过期后可重新登录返回页面。

补充验收：重复注册不产生重复账号；过期或已使用验证链接有明确反馈；重置后旧会话失效；注册者无法访问其他工作区。首次设置只要求工作区名称，可使用默认名称，不强制上传 Logo。

### 4.2 工作台

- 主区域：一句话需求框、“生成活动”按钮及三条真实示例。
- 快捷入口：团队赛马、个人竞速、团队拔河。
- 近期活动：名称、主题缩略图、草稿/已发布状态和下一步操作。
- 现场提示：当前有进行中的房间时优先展示“进入控制台”。
- 首次使用提供样例，不展示无意义的零值指标墙。

验收：新用户能直接开始创建；老用户能快速回到未完成草稿或运行房间。

### 4.3 我的活动

- 字段：名称、场景、玩法、最后修改时间、最新发布版本、房间状态。
- 搜索名称，按玩法和配置状态筛选，服务端分页。
- 动作：编辑、复制为新草稿、发布入口、现场控制、查看报告、归档。
- 运行活动的编辑作用于新草稿，不能改变已运行配置。
- 归档隐藏活动入口，不删除历史成绩；有运行房间时禁止归档。

验收：复制得到独立活动；筛选刷新后保留；空结果可清除筛选。

### 4.4 新建活动

以单页渐进表单降低填写负担：

1. 必填：活动需求描述；允许从模板直接开始。
2. 默认可见：活动名称、预计人数、游戏时长、个人/团队。
3. 选填：Logo、品牌色、产品图、队伍名称。
4. 系统生成后显示推断值，方便确认和调整。

提交后进入编辑器的生成状态，显示“理解需求、编排玩法、准备主题、检查方案”等实际任务阶段。未知耗时不显示虚假进度百分比。

失败可重试或转为手动创建；重复提交按请求标识去重。刷新后通过任务标识恢复状态。

### 4.5 活动编辑器：核心页面

桌面布局：左侧配置区、中央大屏预览、右侧可折叠的 AI 修改区；顶部显示活动名称、保存状态、预览和发布入口。中等宽度下 AI 面板折叠；手机只提供简要查看和现场控制，复杂编辑优先桌面。

配置分组：

| 分组 | 可修改内容 |
| --- | --- |
| 基本信息 | 名称、场景、预计人数 |
| 玩法规则 | 赛马/拔河、时长、输入、队伍、允许的计分选项 |
| 品牌主题 | Logo、配色、背景、已提供的角色主题 |
| 入场展示 | 欢迎词、身份展示、分队说明 |
| 主持内容 | 开场、规则提示、倒计时与结束词 |
| 结果展示 | 标题、贡献卡文案、品牌落款 |

预览支持入场、运行、结算阶段切换，模拟玩家数量，开始/停止试跑及全屏预览。模拟器使用测试输入，不连接正式房间，不产生真实成绩。

AI 修改以具体结果呈现：“队伍由 2 组改为 3 组”“背景由红金改为科技蓝”。候选变更确认后进入草稿，支持撤回。若生成期间用户继续编辑，通过草稿版本检测冲突，不覆盖新修改。

保存采用防抖、串行提交和草稿版本号；展示“保存中/已保存/保存失败”。离开时提示未保存修改。发布前等待保存完成；多人或多标签页冲突返回明确提示，首期不做实时协同编辑。

验收：普通用户无需接触 JSON；手工修改、AI 修改与预览结果一致；浏览器刷新恢复最后成功保存版本。

### 4.6 发布与入场

- 汇总：玩法、队伍、时长、规则、主题、资源清单及主持词。
- 校验：必填配置、规则合法、资源齐全、模拟可结束。
- 阻断错误定位到对应字段；非阻断建议单独呈现。
- “发布版本”仅创建不可变配置，不自动开局。
- 发布后提供“创建现场房间”，生成参与者小程序码、大屏地址和主持人控制入口。
- 显示所用版本号和房间状态，避免操作错场。
- 玩家码、大屏访问凭证与主持人权限相互独立，主持人入口不能作为公开扫码内容。

首期一次活动可以重复创建房间，但管理流程默认一个活跃房间；多房间并行管理后续再扩展。

验收：修改草稿不改变发布版本；重复发布请求不产生无意重复；二维码失败可重试，不误报准备完成。

### 4.7 现场控制台

桌面和手机均可用；减少装饰，突出房间名称、实时连接和主操作。

- 状态区：房间、版本、局次、阶段、剩余时间、已加入/在线人数。
- 准备区：大屏是否在线、资源就绪、队伍人数、规则摘要、小程序码。
- 运行区：开始、暂停、继续、结束并结算、中止本局。
- 反馈区：排行榜、连接异常、命令处理中/成功/失败。
- 辅助区：主持词、近期操作记录、重新打开大屏。

“结束并结算”与“中止本局”分别说明后果，必要操作二次确认。主操作发送后锁定重复点击，等待后端确认；超时显示“状态待确认”并查询，不能直接认为失败而以新请求重复执行。

断线时冻结可执行操作并提示重连；恢复后先拉最新快照。其他主持人产生的操作通过服务端广播同步。首期不在现场修改计分参数。

验收：手机能主持完整一局；断网不出现假成功；当前阶段只显示合法动作。

### 4.8 活动报告

- 按房间与局次选择结果，明确正式、模拟、中止局的区别。
- 概览：去重加入人数、有效参与人数、参与率、游戏时长。
- 参与率口径：有有效游戏输入的玩家数 / 本局锁定的计分成员数；未开局或分母为零显示“不适用”。
- 排行：团队与个人贡献，平局依据发布规则展示。
- 素材：品牌成绩卡生成状态、预览、下载、重试。
- 汇总只展示实际记录的数据；首期不宣称曝光量、转化率或营销 ROI。

验收：重试战报不改成绩；局次切换正确；中止局不伪装正常获奖结果。

### 4.9 玩法模板与品牌素材

模板首期提供赛马、拔河说明和现有主题，展示玩法、手机操作、时长建议、动画预览与“使用模板”。人数标注区分建议规模和已经验证的容量。

品牌素材首期为轻量管理：名称、Logo、主色、产品图和基础介绍。支持在创建页直接上传并复用，不要求先建品牌才能创建活动。编辑品牌不追溯改变已发布版本；已引用资源采用保留引用的处理方式。

## 5. 三类状态必须分开

| 对象 | 状态 | 说明 |
| --- | --- | --- |
| 草稿与生成 | 编辑中、生成中、生成失败、可预览 | 生成失败不等于整个活动失效 |
| 发布版本 | 不可变版本记录 | 新草稿与旧发布版本可以并存 |
| 房间/局次 | 等待、倒计时、运行、暂停、结算、完成、中止 | 控制台以服务端状态为准 |

页面不能用一个“进行中”概括所有情况。例如应明确显示“草稿正在生成 / v2 已发布 / 房间正在运行”。

## 6. API 与实时通信边界

下表是契约草案，编码前与后端定稿。

| 能力 | HTTP 接口草案 | 说明 |
| --- | --- | --- |
| 当前身份 | GET /api/v1/me | 账户、组织与授权范围 |
| 注册/登录/退出 | POST /api/v1/auth/register、/login、/logout | 服务端会话及限频 |
| 邮箱验证 | POST /api/v1/auth/verify-email、/resend-verification | 一次性验证与重发 |
| 找回密码 | POST /api/v1/auth/forgot-password、/reset-password | 统一提示、凭证校验与会话撤销 |
| 活动管理 | GET/POST /api/v1/activities | 查询与创建 |
| 草稿保存 | PATCH /api/v1/activities/:id/draft | 带预期版本，冲突返回 409 |
| 上传准备 | POST /api/v1/assets/uploads | 获取授权上传信息，完成后服务端校验 |
| 生成任务 | POST /api/v1/activities/:id/generations | 绑定草稿版本与请求编号 |
| 任务进度 | GET /api/v1/generations/:id | 首期轮询，可刷新恢复 |
| 配置校验 | POST /api/v1/activities/:id/validations | 返回字段级错误和建议 |
| 发布 | POST /api/v1/activities/:id/releases | 幂等请求，指定精确草稿版本 |
| 房间创建 | POST /api/v1/activities/:id/rooms | 绑定发布版本 |
| 房间快照 | GET /api/v1/rooms/:id | 获取当前权威状态 |
| 现场命令 | POST /api/v1/rooms/:id/commands | 命令编号、局次与预期状态 |
| 房间更新 | WSS /ws/rooms/:id | 快照、阶段、连接和命令结果 |
| 报告 | GET /api/v1/activities/:id/report | 支持房间/局次过滤 |

命令走 HTTP，结果可通过响应和实时通知确认，两者按同一命令编号合并。WebSocket 负责订阅，不另造第二套控制命令。普通生成任务先轮询，减少首期通信复杂度。

服务端返回统一错误码、可读提示、字段错误与请求追踪编号。表单错误就地显示；只有短暂通知使用 Toast，不能用一闪而过的提示代替阻断错误。

## 7. 会话、部署和预览隔离

- 建议管理站点与 /api、/ws 使用同源反向代理，静态资源按内容哈希缓存，入口页及时刷新。
- 管理员采用服务端会话与 HttpOnly、Secure Cookie；写请求验证 CSRF/Origin，WebSocket 握手检查来源和房间权限。
- SPA 路由配置回退至入口文件；API 错误不回退成 HTML。
- 前端不保存模型密钥、微信 AppSecret，不直接连接数据库或调用需要密钥的模型 API。
- 预览通过受控 iframe 加载大屏应用；postMessage 检查精确来源、窗口和消息格式，不用通配来源传递配置。
- 预览只传必要配置或短期预览凭证，不传管理员会话凭证。正式大屏凭证只允许读取指定房间。
- 退出、活动切换和页面卸载清理订阅；预览隐藏后暂停动画与资源消耗。

## 8. UI 设计原则

- 管理界面使用清晰中性色和一个主强调色；客户品牌主题只进入预览与活动资源，不重绘整个后台。
- 首期以浅色工作台为主，现场控制可使用深色模式，确保状态与按钮对比度。
- 一个页面突出一个主任务：创建页生成、编辑器预览发布、控制台运行、报告页查看结果。
- shadcn/ui 提供基础组件，业务能力封装为品牌上传、队伍编辑、阶段提示、发布检查和命令按钮。
- 桌面编辑体验优先；控制台按手机窄屏单独验收，大屏不受后台布局限制。
- 所有表单有可见标签，错误不只用颜色表达，键盘可操作，加载与禁用说明原因。

## 9. 实现批次与验收

| 批次 | 页面 | 对应开发阶段 |
| --- | --- | --- |
| A | 登录、注册、邮箱验证、密码恢复、首次设置、活动列表、固定创建、控制台 | P1 跑通真实游戏 |
| B | 编辑器、模板、品牌上传、发布页 | P2 配置与主题复用 |
| C | 一句话入口、生成进度、AI 修改面板 | P3 生成体验 |
| D | 报告、成绩卡、异常状态、手机控制优化 | P4～P5 验证与试点 |

关键验收：草稿冲突不覆盖、发布等待保存、AI 修改不覆盖新编辑、命令超时可确认、会话过期可恢复、房间切换不串数据、预览无正式副作用。

管理端完成标准：非开发人员独立完成创建、修改、发布、手机主持和查看战报；全程不需要开发者解释内部参数。

## 10. UI 模板候选

本节“模板”指管理端界面模板，区别于产品内的赛马、拔河玩法模板。以下为 2026-09-15 查看官方页面或作者仓库得到的候选，尚未下载、安装或最终选定。

| 方案 | 可复用部分 | 与当前架构的关系 | 建议 |
| --- | --- | --- | --- |
| shadcn/ui 官方 Blocks | 登录、注册、导航及基础页面区块 | 可按需接入 Vite，保留现有 React Router + FastAPI 设计 | 首选组合式搭建 |
| Satnaing Shadcn Admin | 完整后台布局、设置、列表、响应式与深浅主题 | Vite + shadcn，现有 TanStack Router 和部分 Clerk 集成需适配 | 适合参考整体视觉与页面结构 |
| Kiranism Next shadcn Dashboard Starter | Next.js 后台、表单、表格及认证相关结构 | 需要重新评估 Next.js 及认证依赖，与当前 Vite 方案不同 | 保留为 Next.js 备选 |

### 可直接查看

- [官方登录区块](https://ui.shadcn.com/blocks/login)
- [官方注册区块](https://ui.shadcn.com/blocks/signup)
- [官方区块集合](https://ui.shadcn.com/blocks)
- [Shadcn Admin 演示](https://shadcn-admin.netlify.app/)
- [Shadcn Admin 作者仓库](https://github.com/satnaing/shadcn-admin)
- [Next.js Dashboard 作者仓库](https://github.com/Kiranism/next-shadcn-dashboard-starter)

### 当前推荐

使用官方登录/注册区块和导航组件构建基础页面，参考 Shadcn Admin 的布局与状态处理；EventPlay 的活动编辑器、游戏预览和现场控制按业务定制。

登录与注册建议统一分栏：一侧表单，一侧 EventPlay 游戏画面与一句话介绍；手机只保留品牌标识和表单。所有演示按钮必须接入真实服务或移除。

Shadcn Admin 的作者明确将其定位为 UI 集合而非完整 starter，其认证并非 EventPlay 所需的完整后端。选用页面不能替代 FastAPI 的会话、权限、邮箱与密码恢复实现。若完整采用该项目，应明确选择迁移其路由或调整本项目路由计划，避免同时保留两套路由。

两份第三方仓库当前标注 MIT；正式引入时记录具体版本、保留许可证并核对使用到的素材来源。模板选择不会自动改变当前技术栈。

## 11. 官方技术参考

- [shadcn/ui 的 Vite 接入](https://ui.shadcn.com/docs/installation/vite)
- [React Router 模式说明](https://reactrouter.com/start/modes)
- [TanStack Query 官方快速入门源码文档](https://github.com/TanStack/query/blob/main/docs/framework/react/quick-start.md)
- [React Hook Form](https://www.react-hook-form.com/)

以上资料用于确认技术能力；页面划分、通信约定和迭代顺序是 EventPlay 的设计方案，尚未实现。
