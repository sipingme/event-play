# 控制游戏（本地基础可玩版）

模板库「控制」共 8 款。支持模板预览、独立扫码试玩、草稿与发布快照、玩家控制、战队大屏、暂停/恢复、结束/中止与重赛。此次未部署服务器、未提交 GitHub。

| 模板 | 操作 | 判定 |
|---|---|---|
| control-coins 接住好运 | 左右移动篮子 | 同轨金币 +1、炸弹 -1 |
| control-runner 萌宠跑酷 | 左右换道、跳跃 | 星星 +2、安全通过 +1、撞障碍 -1；跳跃保护当前波，不可连续两波跳跃 |
| control-space 太空护航 | 左右按钮或拖动换道，自动射击 | 同轨敌机 +2、碰陨石 -1 |
| control-ski 极速滑雪 | 左右转向 | 穿旗门 +2、撞障碍 -1 |
| control-boat 小船寻宝 | 左右选航道 | 宝箱 +2、礁石 -1 |
| control-parking 欢乐停车场 | 左/右转向、前进、后退 | 绕障碍，车头朝上停入右上角 P 位 +5 |
| control-maze 迷宫寻宝 | 四向移动 | 先拿右下角钥匙，再到右上角出口 +5 |
| control-balance 平衡小球 | 四向移动、刹车 | 连续同向滑两格，刹车清除惯性；终点 +5、落洞或越界 -1 并回起点 |

所有扣分最低到个人 0 分，团队同步扣除实际分值。方向按钮支持按住连续操作，松开、失焦、隐藏页面或暂停时停止。浏览器键盘可聚焦按钮后按空格/回车操作。

## 服务端规则

客户端只发送递增序号和方向，不提交坐标或分数。服务器限制操作频率、边界、墙体、重复序号及允许的方向。前五款按服务端波次结算，避免前端重复提交碰撞刷分。房间状态和玩家位置随原有存储流程持久化。

`controlVariant` 随模板、草稿、发布、试玩与重赛保留；旧版没有此字段的 catch 活动仍走原接金币规则，但复用新的卡通界面，不添加炸弹以免改变已发布规则。

## 美术

统一赛马系列的明亮 2.5D 卡通环境、奶油色信息牌、暖橙标题及战队成绩。接好运使用财富小镇及新生成金色篮子；跑酷使用新萌宠；太空、雪道、海湾、停车复用已有同系列场景与角色。

新素材由内置 imagegen 生成，版本化保存在 `apps/admin/public/games/control/`，提示词见该目录 `ART-DIRECTION.md`。金币、炸弹、旗门、宝箱等小道具使用代码原生渐变 SVG，平衡球使用代码原生球体；无远程素材依赖。

## 当前边界

这是 8 款可联机试玩的轻量实现，不是完整物理游戏：前五款为三轨波次规则；停车/迷宫为 5×5 网格关卡；平衡球为网格惯性规则，不调用手机倾斜传感器。飞船自动射击在波次落点判定，不做逐像素弹道。网格地图当前固定，过关重置并累计成绩。

大屏展示战队角色和汇总成绩，不展示几百名玩家的全部个人位置。现有最小后端仍是单 worker 模式，本次没有进行数百人真实并发压测或手机硬件实测；商用前仍需压测、网络延迟体验与安全检查。

## 验证

- `apps/api`: `python -m unittest discover`
- `apps/admin`: `npm test`、`npm run build -- --webpack`
- `tests/browser-control.cjs`: 八款预览、窄屏、真实操作得分、暂停、从模板创建。
- `tests/browser-templates.cjs`: 原五类示例回归。
