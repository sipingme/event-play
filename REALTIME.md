# EventPlay 实时联调 MVP

## 本次实现

### 答题闯关、基础抽奖、接金币（本地新增，未部署）

当前共八类 H5 玩法。新增三类共用现有工作区、房间权限、暂停、结束、报告与再来一局流程。

- `quiz`：编辑器可配置1～20道四选一题；每行格式 `题目|A选项|B选项|C选项|D选项|正确字母`。总时长均分到各题，每题至少5秒（整数秒显示时可能存在一秒取整差）。每题只能提交一次，未开场／暂停／过期／提前答未来题均拒绝；本题结束或主持提前结算时统一判分，答对+10。公开房间快照不包含题库原文、正确答案或未来题目，管理端授权活动配置保留原题库。中止不补计当前尚未结算的题目。
- `draw`：配置1～100个中奖名额，不得超过预计人数。至少足够人数入场才能开始；开场后禁止新入场。主持点击「抽取并锁定结果」，使用服务端 SystemRandom 不放回抽样。保存候选身份ID、中奖ID／昵称、抽取时间及算法标识；重复开奖返回原结果。超时、提前结束或中止而未开奖，显示“本局未开奖”，不会自动产生中奖人。结果随 JSON 成绩导出。
- `catch`：三轨道简化接金币，每2秒一枚金币，所有玩家使用同一落点序列，各自控制篮子；初始位于中轨，每次左右移动一格。服务端判断金币落地时篮子轨道是否一致，一枚+1分，同一枚只结算一次。移动至少间隔150毫秒且带递增序号，暂停时冻结。客户端仅做掉落动画，不可自报命中或最终成绩。

边界：题目需主办方审校，不自动抓取事实或AI出题；抽奖不涉及奖品库存、兑奖、支付或独立随机性公证；游客仍可伪造多个身份，不能视为实名公平抽奖。新增系列内游客身份去重、答案揭晓与接金币难度详见文末。接金币为离散轨道玩法，并非自由物理引擎，未做弱网公平性或大规模压测。三项均为 H5，原生小程序未同步适配。

专项后端测试覆盖答题答案脱敏、未来／重复／过期答案、延迟统一计分，抽奖主持权限、人数限制和结果锁定，接金币碰撞、漏接、重复结算及禁止点击刷分。后端共13项测试；浏览器脚本增加三种新玩法及成绩导出／新局验证。

### 新增左右交替、共同点亮与主持操作优化（本地，未部署）

- `alternating`：左右按钮交替操作，每次有效输入 +1；服务器拒绝连续同侧输入。暂停或被限速的输入不改变已接受的左右顺序。
- `light`：汇总所有战队分数作为共同能量，编辑器可设置目标 10～100000，默认 1000；达到目标时服务器自动结算。大屏逐渐点亮上传的 Logo，未上传时使用品牌文字。超时未达到目标明确显示未达成，不宣布队伍输赢。
- 已结束房间可“相同规则再来一局”：新房间使用上一局配置快照、清空玩家与分数，旧局保持不变，玩家必须扫描新二维码重新入场。重复请求同一旧局返回同一新局。云活动已有未结束局或被归档时拒绝另开。
- 主持端支持下载本局成绩 JSON，包含房间、活动、队伍和玩家贡献，无主持／玩家令牌；不是 CSV 或奖品发放记录。
- 输入增加同步请求锁，避免同一渲染周期重复发送；页面切换房间时重置玩家／主持视图状态。
- 403 提供工作区接管／站点访问检查提示。房间状态无变化时减少重复快照，两秒保活，变化最多每 200ms 检查一次。不代表已完成并发压测或支持千人承载。
- 当前 H5 支持竞速、拔河、数钱、左右交替、共同点亮五类规则。原生小程序仍仅支持早期点击规则，不宣称与 H5 全量同步。

验证新增：交替顺序、暂停恢复、共同目标完成、结束后拒绝计分、重新开局幂等与权限隔离。后端共 10 项测试；前端单测与生产构建。浏览器验收脚本扩展为数钱、左右交替、点亮目标、成绩下载和重新开局。

### 云工作区、房间管理与数钱（本地新增，尚未部署）

入口：管理端侧栏「云工作区与房间」(`/dashboard/cloud`)。

1. 创建工作区后立即复制并私下备份管理密钥；换浏览器在同页连接已有工作区。
2. 可以主动导入本机实际保存的活动为云草稿；不删除本地数据，不迁移旧房间或模拟报告，不保留本地发布状态。重复导入同一活动不覆盖云端修改。
3. 活动列表、创建、修改、发布和归档在连接云模式后调用后端。并发保存通过 revision 防止旧版本覆盖；开局使用发布快照。
4. 同一活动复用未结束的房间；结束后生成下一局。房间历史展示轮次和入场人数，可查看大屏与成绩。
5. 重新进入或接管会轮换房间主持凭证，原主持浏览器下一次操作会被拒绝。工作区密钥与房间凭证不同，玩家不能获取工作区密钥。
6. 数钱模板使用 `money` 规则；H5 向上划动至少 48px、方向以垂直为主且在 2 秒内完成，提交 `kind=swipe`。无障碍按钮提交同类型事件。服务端校验输入类型、序号、阶段和频率，每次有效输入 +1；不接受客户端最终分数。

重要边界：管理密钥是预览版 capability，不是邮箱注册／密码登录，也没有丢失找回或密钥撤销界面。工作区创建仍受整站预览访问保护，不能移除站点门禁直接商用。工作区内品牌素材库、旧彩排与报告仍为本地数据。数钱仅验证 H5，原生小程序暂未适配。服务端不能证明真实手指运动，客户端可以伪造 swipe 事件；不能用于高价值奖品竞争。

数据库升级为增量建表：新增 workspaces、activities，rooms 的旧记录保持兼容。服务器只保存管理密钥哈希。上限：每实例 1000 工作区，每工作区 200 活动，实例总房间仍为 1000。尚未做并发容量承诺。

验收：后端 8 项测试覆盖租户隔离、重复导入、过期修改、发布快照、跨设备接管、轮次和持久化；前端 5 项单测。`apps/admin/tests/browser-cloud.cjs` 使用隔离内存后端 8012 和网页 4191，验证创建工作区→创建数钱活动→发布→玩家加入→主持开始→真实指针划动与无障碍操作计分→结算→房间历史。运行时通过 PLAYWRIGHT_MODULE 指向已安装的 @playwright/test，并需要本机 Chrome。

### 2026-09-16 联机入口统一

- 管理端发布页和主持端首页默认进入联机主持台，同一浏览器按活动复用尚未结束的房间；已结算或中止时创建下一局。
- 旧彩排保留为明确标注的次要入口，不能接收真实玩家。
- 主持台和等待中的大屏提供本地生成的玩家二维码，主持台可复制玩家链接。
- 玩家等待页展示房间 ID、入场成功和队伍；主持台说明开始按钮不可用的原因。
- 主持凭证可私下复制备份，在同一房间的另一浏览器粘贴后由服务器验证。凭证等同控制钥匙，不得分享给玩家；这不是账号找回。
- 活动与房间关联仍存储在当前浏览器；旧房间不会自动补关联。已有旧联机房间请继续使用原 `/live/host/{id}`，不要另建房间。跨设备活动同步、正式账号及丢失全部凭证后的恢复尚未实现。

- `apps/api`：Python/FastAPI + WebSocket 状态流 + SQLite 持久化。单进程运行，无 Redis/Kafka。
- `apps/admin`：主持人入口新增“新建联机房间”，独立联机主持台、大屏与手机网页玩家端。
- `apps/miniprogram`：微信小程序开发工程，支持房间读取、测试昵称、队伍选择、点击贡献、暂停、结算及重连。
- 旧 localStorage 彩排仍保留；联机局不使用模拟分数，不混入旧模拟报告。

**开发联调版，不是可直接商用的多人活动服务。小程序采用游客测试身份，并未接入 wx.login/code2Session。网页是联调替代入口，不取代正式小程序产品。**

## 本机启动

终端一：

```powershell
cd D:\2026\eventplay\apps\api
python -m venv .venv
.\.venv\Scripts\python -m pip install -r requirements.txt
.\.venv\Scripts\python -m uvicorn main:app --host 127.0.0.1 --port 8001 --workers 1
```

终端二：

```powershell
cd D:\2026\eventplay\apps\admin
npm run dev -- --hostname 127.0.0.1 --port 4180
```

1. 打开 `http://127.0.0.1:4180/host`，选择已保存演示版本的活动，点击“新建联机房间”。
2. 在联机主持台点击“打开玩家端”，输入测试昵称和队伍。可用另一浏览器/无痕窗口测试第二名玩家。
3. 主持人确认开始。玩家点击后，主持台与联机大屏显示服务端分数。
4. 暂停不计分，恢复后继续；时间到自动结算，也可提前结束。中止不评定胜负。

页面地址：`/live/host/{房间ID}`、`/live/play/{房间ID}`、`/live/screen/{房间ID}`。
旧 `/screen/{id}` 和 `/host/{id}` 仍是纯本地彩排，并不会自动变成联机局。

## 微信开发者工具

导入 `apps/miniprogram`，使用测试 AppID/游客模式（当前 project.config.json 是 touristappid）。
默认后端 `http://127.0.0.1:8001`，仅适用于同机开发者工具。在开发环境关闭合法域名校验；不要将此设置用于正式发布。
填写主持人页面的房间 ID。也可用编译参数 `room=房间ID` 打开 `pages/play/play`。
工程未在微信开发者工具及真机验收，不能宣称小程序已上线或微信登录可用。

## 手机局域网测试（手动开启，不自动修改防火墙）

手机和电脑在同一可信 Wi-Fi。将下面 `192.168.1.10` 换成电脑局域网 IP。

```powershell
# 后端：仅可信局域网使用。创建房间和游客加入没有账号认证。
$env:EVENTPLAY_ORIGINS='http://127.0.0.1:4180,http://localhost:4180,http://192.168.1.10:4180'
python -m uvicorn main:app --host 0.0.0.0 --port 8001 --workers 1
# 前端（另一个终端）
npm run dev -- --hostname 0.0.0.0 --port 4180
```

手机使用 `http://192.168.1.10:4180/live/play/{房间ID}`，不能使用 127.0.0.1。
浏览器默认连接当前主机的 8001 端口；可用 `NEXT_PUBLIC_REALTIME_URL` 覆盖。小程序在 `app.js` 修改 apiBase。
需要系统防火墙允许可信局域网访问 4180/8001。不要将端口暴露到公网。

## 状态与安全边界

- 房间创建返回独立随机主持凭证，保存在创建浏览器；玩家凭证无权开局或暂停。丢失本机存储则无法恢复主持权限（尚无账号找回）。
- 玩家令牌按房间隔离；公开快照不含令牌或哈希，仅含测试昵称、队伍和贡献。持有房间链接即可观看，请勿填写个人隐私。
- 点击请求为 HTTP，每条含递增序号；重复序号不重复计分，每玩家令牌最多约 10 次/秒，允许初始短突发。WebSocket 每 200ms 推送权威状态。
- 无客户端自报分数；未开局/暂停/结束点击不计分。断线不缓存补发，恢复后读取权威快照。
- SQLite 保存房间和成绩。服务进程重启后进行中的比赛暂停，若截止时间已过则结算。暂停前最多不足 1 秒的取整偏差，适用于 MVP。
- 同一玩家身份不支持多标签并行输入。游客可建立多个身份，基础限速不能替代防刷/实名身份。
- 仅支持单 worker；没有分布式锁、Redis 广播、压测承诺、在线心跳人数或正式奖品逻辑。显示的是“已入场人数”，不是实时在线人数。
- 自动记录最近 50 条主持命令。正式结果目前查看联机主持台/玩家端，未并入管理端报告列表。

## 验证与下一步

后端测试：`python -m unittest -v test_main.py`。覆盖控制权限、令牌脱敏、WebSocket、队伍校验、重复序号、限速、暂停/恢复、禁止迟到加入、到时结算与 SQLite 重载。
前端：`npm run typecheck`、`npm run build -- --webpack`，旧彩排测试 `npm test`。

上线前必须接入管理账号与房间权限、微信 code2Session、HTTPS/WSS 合法域名、入场凭证、反刷、隐私说明、监控、负载测试、正式报告和部署配置。
WebSocket 实现参考 [FastAPI 官方说明](https://fastapi.tiangolo.com/advanced/websockets/)。

## 整场编排与游戏细节（本地新增）

- 新增玩家/大屏心跳：前台每5秒上报，服务端15秒过期。玩家持房间凭证，按玩家ID去重；大屏是公开页面自报连接，不能证明真实投影。主持端显示入场、近期在线、未检测到心跳及大屏页面数。完整边界和真机待测清单见 `FIELD_ACCEPTANCE.md`。

- 主持台新增整场控制：显示当前／下一环节，当前结束后直接推进，已过期的主持页可接管当前环节。需要当前浏览器连接正确云工作区；工作区密钥仍负责整场权限，房间凭证仅控制单局。
- 主持开场前手动确认大屏投放、音量与规则；这些不是自动在线检测。界面点击开始后发送 `countdown` 命令，服务端倒数3秒期间关闭新入场、不计分；允许取消重新开放入场，中止则不再开场。倒数期间重复开场拒绝。服务重启取消待开场倒数，需主持重新确认。
- 原 `start` API 保留立即开场兼容行为；倒数功能由新 `countdown` 命令提供。新主持界面默认使用倒数。大屏／玩家读同一个服务端倒数值，网络延迟仍可能导致视觉时间差。

- 云工作区新增整场编排：选择已发布活动，添加、移除、上移环节，最多20个；保存时锁定发布版本快照。主持人手动推进，当前环节未结束不可进入下一环节；请求带当前索引，拒绝重复推进。跨浏览器可用工作区密钥恢复并接管当前环节。
- 每个环节使用独立房间。整场新增固定玩家入口 `/live/event/{agendaId}` 和固定大屏 `/live/event-screen/{agendaId}`：每2秒同步当前环节，无需重复扫码。未开场显示等待，环节之间保留成绩，最后一环结束提示整场结束。玩家仍需确认队伍并加入新环节，不自动替用户报名；同场记忆昵称，队伍按名称匹配而非旧编号。原单房间链接保持兼容，但不会自动换场。
- 固定入口断网时显示同步异常并允许重试；房间原有重连不变。整场入口只公开名称、当前环节与房间ID，不公开配置、未来题目或管理密钥。尚无统一签到、编排删除/修改及跳过未执行环节。
- 同一编排或“再来一局”系列抽奖，排除已中奖游客凭证。凭证在浏览器保存，服务端只保存哈希，不公开身份哈希。不同昵称不会影响身份；同名不同身份不会误排除。清缓存/换设备仍可重新注册，不是微信实名防刷。独立新建的房间不共享去重范围；旧版本未记录身份的中奖记录不能追溯去重。
- 抽奖支持奖项名称；候选不足时拒绝开奖，不自动减少名额。结果包括奖项、候选名单及中奖名单，无实际发奖。
- 知识题仅揭晓已经结束的题目；提前结算只揭晓已展示题目，不公开未来题目答案。中止不额外公布未结算题目。
- 接金币难度：简单3秒、标准2秒、挑战1秒一枚；由服务器判定，前端插值显示动画。
- 大屏支持全屏切换、服务器剩余时间、最后10秒提示音及结算领先队伍展示；并列不强行选冠军，零分不宣布获胜。音效需要点击启用，黑屏、暂停和断线时不播放。不是开场3秒倒数，也不包含音乐素材。
- 本轮仍是单进程可信预览实现，未部署；不代表生产级并发、真实身份或奖品发放能力。
