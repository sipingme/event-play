# 默认游戏体验库（首批五类）

## 疯狂数钱视觉与交互优化（本地）

- 独立财富大屏：全场总分、2–4 队财富积分、领先/并列排名、积分卡堆叠、等待扫码、暂停/断线/中止/结算状态和场景全屏。
- 手机卡片跟随拖动并在提交后飞出；提交提示不代表加分，个人贡献仅使用服务端确认值。保留向上至少 48px、2 秒内完成、无障碍按钮和原有服务端计分/限速。
- 卡面为可实时替换品牌文字的 CSS 图形，明确标注虚拟积分与非现金；不是货币或现金奖励。卡堆数量封顶为视觉示意，不改变真实计分。
- 尊重减少动态效果设置。构建、9 项前端测试、新增数钱浏览器验收以及五类示例回归通过；修复手机预览比例撑宽导致的裁切。

## 2026-09-17：滑屏首批四例（本地）

- 疯狂数钱：沿用财富滑滑乐的模板 ID `swipe-money`，向上滑动，每次有效操作 +1 虚拟积分。
- 龙舟竞渡：`swipe-dragonboat`，向下划桨。
- 自行车冲刺：`swipe-bicycle`，左右交替滑动，服务端拒绝连续同侧。
- 步步高升：`swipe-climb`，向上滑动，纵向攀登；每 10 分点亮一层，最多显示 8 层，比赛仍按最终积分排名。

三个新例子复用限时团队竞速，配置包含 `inputMode=swipe` 与 `swipeDirection`，保存到草稿、发布和房间快照。浏览器要求至少 48px 且 2 秒内完成，检查主要移动方向；服务端校验输入类型、交替顺序、状态、序号和限速，不声称能够验证物理手势真实性。无障碍按钮提交相同方向事件，不能绕过交替规则。

卡通素材使用内置 imagegen，提示词见 `apps/admin/public/games/race/illustrated/SWIPE-ART.md`。龙舟与自行车复用现有海湾和公路背景；楼层为实时绘制。品牌拼图暂不实现，需独立设计群体拼合规则。

验收：前端 9 项、后端 30 项测试通过，三个新例子的实际浏览器划动、错误方向、暂停、配置保存及桌面/手机大屏验收通过。生产构建通过。尚未部署或推送。

## 2026-09-17：摇一摇场景扩展（本地）

摇动竞速现在有 8 个可选场景：赛马、游艇、赛车、摩托、星际飞船、火箭、企鹅滑雪、热气球。新增七款现已升级为与赛马统一的明亮 2.5D 卡通插画：每款独立背景与透明角色，游戏库、编辑预览、试玩和正式大屏共用。美术采用内置 imagegen 制作，提示词和资产说明见 `apps/admin/public/games/race/illustrated/ART-DIRECTION.md`。

- `raceVariant` 随草稿、发布快照、试玩、房间及下一局保存；未配置的旧活动保持赛马。
- 火箭与热气球纵向上升，其他横向竞速；水花、烟尘、推进尾焰、雪迹及浮动效果随运行状态播放。
- 都是限时积分赛，最高积分获胜；位置仅表示相对积分，不是物理距离或完成百分比。
- 场景可在活动编辑器切换；保留摇动权限、点击备用、暂停/重连冻结、并列排名和减少动态效果支持。
- 首页仍按五类各推荐一个，完整摇动系列在游戏体验库的“摇一摇”分类中选择。

入口：`/dashboard/templates`。首页优先呈现五个可玩示例，先体验，再创建独立品牌活动。

| 类型 | 模板 ID | 游戏 | 实际规则 |
| --- | --- | --- | --- |
| 摇一摇 | shake-race | 欢乐摇摇赛马 | 动作传感器检测摇动；同分点击备用；共享服务端输入限速 |
| 滑屏 | swipe-money | 财富滑滑乐 | 向上滑动至少 48px；每次有效操作 +1；支持无障碍备用按钮 |
| 点击 | click-sprint | 左右冲刺赛 | 左右交替有效，连续同侧不计分 |
| 手眼协调 | reaction-mole | 萌鼠出没 | 九宫格，每 2 秒一个服务端随机目标；每人每轮只能提交一次；命中 +1 |
| 控制 | control-coins | 接住好运 | 三轨道左右移篮，每枚金币落地由服务端判定，接到 +1 |

## 体验流程

1. 按操作类型筛选游戏，查看效果预览（明确标记示意成绩）。
2. 点“创建扫码试玩”：创建独立房间，不创建活动草稿、不关联正式活动。
3. 右侧浏览器玩家输入昵称，或手机扫码；入场后由体验者点击“开始试玩”。
4. 真实玩家输入通过原有 API/WebSocket 同步到左侧大屏；可暂停、继续、结束。
5. “用此模板创建”打开活动编辑器，继承玩法、队名、主题、时长及摇动设置；保存、发布后再创建正式联机房间。

首批试玩为 30 秒、最多 10 人，10 分钟有效期。到期在房间访问或实时广播时中止；历史试玩房间保留，不自动删除。旧有答题/抽奖模板沿用自身最低题目时长/中奖人数约束，可能超过此默认。每次创建及重新试玩使用独立房间。试玩不是正式战报，不发奖品。

## 手机兼容边界

- 摇动需要支持 DeviceMotion 的设备；显式点击按钮申请权限，拒绝或无传感器时可点击备用。
- [MDN DeviceMotion 权限说明](https://developer.mozilla.org/en-US/docs/Web/API/DeviceMotionEvent/requestPermission_static)：涉及安全上下文和用户手势，浏览器支持并不一致。
- 自动化覆盖模拟动作事件、点击备用、真实滑动轨迹、交替点击、命中判定与移动接物；不是实机传感器兼容性认证，iOS/Android/微信内置浏览器仍需现场验收。
- 本机 localhost 二维码不能用于其他手机；使用线上 HTTPS 站点。线上预览访问保护保持开启。

## 测试

- 前端：`npm test`，模板分类完整性与配置快照。
- 后端：`python -m unittest test_main`，新增命中/误击/重复/过期目标/暂停/凭证、摇动备用共享序列、试玩隔离及过期测试。
- 浏览器：`tests/browser-templates.cjs`，隔离 UI 4191 / 内存 API 8012；验证五类从游戏库→试玩→玩家操作→暂停/结束→模板创建流程。
- 不复制竞品美术；赛马使用已有原创生成素材，其余使用代码绘制的基础视觉。AI 自由生成、正式微信身份和真实奖品发放不在本次范围内。
