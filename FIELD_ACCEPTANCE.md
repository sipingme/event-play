# EventPlay 现场验收与容量验证

## 当前状态

心跳每5秒上报，15秒过期，公开快照最多再延迟2秒显示。玩家必须持有该房间有效凭证；多标签按玩家ID去重。大屏按页面标识计数，最多20个。页面隐藏时停止心跳。重启后心跳不恢复，必须收到新心跳。

大屏心跳是公开页面的自报连接状态，不是受信设备认证、投影画面识别或音量检测。尚未进行微信/iOS/Android真机验证，也未确定生产容量。

## 真机清单（均待人工执行）

在 iOS 微信、Android 微信、Safari、Chrome 分别记录设备、系统、浏览器版本和活动ID。

1. 扫码入场，主持端15秒内显示近期在线；昵称、队伍正确。
2. 锁屏20秒／切后台20秒：主持端显示未检测到心跳；返回后恢复在线，身份与分数不变。
3. Wi-Fi切移动网络、断网20秒后恢复：不能补发离线点击，不能重复计分；恢复后仍是同一玩家。
4. 倒计时期间断网、暂停时刷新、结算后刷新：状态最终与服务器一致，不能提前计分。
5. 主持切下一环：玩家与大屏自动换场；确认新队伍后加入，不要求再次扫码。
6. 关闭大屏页面，约17秒内页面计数减少；重新打开后恢复。验证投影和音量需人工目视、试听。
7. 两个标签打开同一玩家：在线人数只算一次；关闭一个标签另一个仍维持在线。
8. 测试服务重启：倒计时取消；活动中恢复规则符合 REALTIME.md；恢复后无身份泄漏。

## 隔离压测

只对临时数据库、独立端口执行，不使用真实活动数据。`apps/api/load_inputs.py` 默认仅允许 localhost。50、100、300、500人分别运行并保存输出。脚本当前仅测试HTTP输入（最多100并发线程），报告延迟、错误和成绩一致性；不能代表500个WebSocket广播接收端的负载。

后续完整容量测试仍需：每玩家建立WebSocket并收包、测端到端广播延迟、持续至少10分钟、监控CPU/内存/SQLite写延迟、加入慢连接与断线重连，再在目标服务器复测。未完成前不宣称可承载500人现场活动。

### 本轮本地测量

环境：Windows 本机、单 Uvicorn worker、SQLite `:memory:`、每玩家5次请求、最大100个HTTP线程、无WebSocket订阅者；延迟不包含线程队列等待。总用时包含入场阶段。不是磁盘持久化或生产服务器测量。

| 注册玩家 | HTTP请求 | 错误 | P50毫秒 | P95毫秒 | 总用时秒 |
|---|---:|---:|---:|---:|---:|
| 50 | 250 | 0 | 63.91 | 93.23 | 0.77 |
| 100 | 500 | 0 | 127.92 | 170.10 | 1.52 |
| 300 | 1500 | 0 | 195.37 | 227.30 | 4.20 |
| 500 | 2500 | 0 | 222.10 | 267.64 | 7.19 |

四档请求均全部被接受，服务端总分与接受数相等。仅为短时输入基线，不据此作现场容量承诺。

## 仍待完成

## WebSocket 联合短测（本轮新增）

脚本 `apps/api/load_realtime.py`：只允许 localhost，每位玩家一条WebSocket；每轮同时提交一次输入并等待所有接收端总分收敛；首轮后主动重连10%的连接。报告包含HTTP延迟、输入到自身成绩快照延迟、重连首包延迟、版本倒退和意外断线。异常时输出失败阶段和计数。运行命令例：`python load_realtime.py --players 100 --rounds 3`。

环境仍是同一台Windows机器上的压测客户端与单worker服务端，SQLite内存数据库，3轮短测，无模拟慢接收端、无心跳并发负载。HTTP和广播延迟包含客户端排队时间，与上表纯HTTP指标不可直接比较。

首次测试：50、100人通过；自身快照P95分别305.39、306.35毫秒。300人发生HTTP ReadError；500人发生ReadTimeout。不能仅归因服务端，客户端事件循环与同机资源竞争尚未隔离测量。

随后新增同房间共享序列化快照缓存（最多每200毫秒更新，空闲2秒刷新），避免每条WebSocket单独构造和编码同一状态。双连接更新一致性和脱敏测试已加入，后端24项测试通过。

| 缓存后复测 | 结果 | HTTP P95毫秒 | 自身快照P95毫秒 | 重连首包P95毫秒 |
|---|---|---:|---:|---:|
| 50人，3轮 | 通过，150次计分一致 | 72.34 | 414.68 | 4.06 |
| 100人，3轮 | 通过，300次计分一致 | 240.54 | 461.27 | 5.13 |
| 300人，3轮 | 未通过，第3轮ReadError | 不报告成功指标 | — | — |
| 500人，3轮 | 未通过，ReadError | 不报告成功指标 | — | — |

300人失败前约15.96秒，已收到17239条、406031444字节JSON消息，确认接受819次输入；部分请求结果未知，不报告最终成绩一致。缓存减少重复编码但未消除全量广播成本，低档延迟也没有改善，不能表述为性能验收完成。

下一步：按角色裁剪玩家数据或增量广播；分机生成负载并采集CPU、内存和事件循环延迟；使用磁盘数据库、慢接收端、10分钟持续运行及目标服务器复测。所有生产容量声明仍待验收。

## 按端裁剪广播复测

新增 `view=player` 与 `view=screen`。玩家通过WebSocket首条消息提交凭证（不放URL），只收自己的明细；大屏只收展示汇总，人数与轨道人数单独统计。主持端保持全名单。原HTTP快照和默认/full广播仍兼容公开房间能力，不能将这次性能裁剪描述为全面的隐私权限改造。玩家与主持/大屏前端缓存已按端隔离。

`load_realtime.py` 默认使用玩家投影，`--view full` 可跑原全量广播作对照。每档仍为相同注册人数、3轮输入、10%重连；同机、内存数据库、不含持续心跳或额外主持大屏连接。

| 玩家投影复测 | 结果 | 自身快照P95毫秒 | HTTP P95毫秒 | 接收JSON字节 |
|---|---|---:|---:|---:|
| 50人 | 通过，150次计分一致 | 425.20 | 96.71 | 233367 |
| 100人 | 通过，300次计分一致 | 428.16 | 169.09 | 418180 |
| 300人 | 通过，900次计分一致 | 1541.53 | 1328.47 | 4030776 |
| 500人 | 未通过，第2轮ReadError | — | — | 8906736（失败前） |

成功档均无意外断线或版本倒退；300人重连首包P95为5.17毫秒。500人失败前8.88秒，确认接受840次，部分请求状态未知。300人的功能一致性通过不代表体验达标，1.54秒反馈延迟仍需优化。下一步应分机复测、分析HTTP输入及SQLite全房间写入成本，再决定批处理/增量持久化策略，不直接牺牲恢复一致性。

本轮后端25项回归测试通过，包含裁剪数据、玩家握手凭证、人数及轨道汇总校验。压测使用默认空Logo，含大型品牌素材的持续广播负载尚未覆盖；分端裁剪不代表所有字段都已优化。

## 后续视觉工作

三款代表游戏第一轮视觉打磨已落地，使用CSS与SVG，不引入新运行依赖或远程素材：

- 竞速：分队色、独立赛道、领先标记、贡献积分、跑动/速度线；暂停和中止不继续跑动，中止不评胜负。赛道位置仍是相对积分示意，明确不表示完成百分比。
- 答题：大屏双列选项、大字号题目、服务端剩余时间环；换题过渡；仅显示服务端已揭晓答案。手机选项加大触控面积和字母标识，不改变答题规则。
- 抽奖：品牌奖项牌、聚光背景、候场呼吸效果、真实中奖卡片入场。暂停、未开奖、已开奖分别展示，不滚动虚构候选人，不改变随机算法。手机和大屏共用结果视觉。
- 沿用金色、太空、花园三套主题变量；支持窄屏换行与 `prefers-reduced-motion`。大屏移除竞速/抽奖下方重复队伍卡片。

视觉验收脚本 `apps/admin/tests/browser-visual-games.cjs` 使用真实隔离房间，截图保存在操作系统临时目录。覆盖三款桌面及390px窄屏无横向溢出、暂停/中止/开奖状态、减少动态设置和手机答题提交。未将此轮视觉工作描述为物理动画引擎、3D场景或专业原画制作。
